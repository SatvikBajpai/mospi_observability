# Ignore Python cache
__pycache__/
*.py[cod]

# Ignore log files
*.log

# Optional: ignore virtual environments
venv/
.env

# macOS files
.DS_Store

# Trace data — never commit (large, may contain user query text / IPs)
all_traces.json
traces.jsonl
*.jsonl
archive/
